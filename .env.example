# Copy the per-workspace templates instead of using this root file:
#   cp apps/engine/.env.example   apps/engine/.env
#   cp apps/web/.env.example      apps/web/.env
#   cp contracts/.env.example     contracts/.env
